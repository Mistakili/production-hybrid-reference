import type { CapacitorConfig } from '@capacitor/core';

const config: CapacitorConfig = {
  appId: 'com.productionhybrid.pushdemo',
  appName: 'Push Module Zero',
  webDir: 'dist',
  ios: {
    contentInset: 'automatic',
  },
};

export default config;