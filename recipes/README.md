# Recipes

Copy-paste production recipes. Each recipe is **independent** — grab one without cloning the full monorepo.

| Recipe | Description |
|--------|-------------|
| [`push-notifications/`](push-notifications/) | Registration, permissions, token refresh, environment tagging |
| [`appdelegate/`](appdelegate/) | iOS AppDelegate + `cap sync` survival workflow |
| [`apns/`](apns/) | Node.js APNs delivery with retry and error handling |
| [`revenuecat/`](revenuecat/) | Webhook handler (planned) |
| [`logging/`](logging/) | Structured logging (planned) |
| [`deep-links/`](deep-links/) | Cold start deep links (planned) |

Start with [`push-notifications/`](push-notifications/) if you found this repo from Google.