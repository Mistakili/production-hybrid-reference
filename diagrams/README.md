# Diagrams

Architecture diagrams for the Production Hybrid reference.

Add Mermaid or exported PNG/SVG diagrams here as modules are implemented.

Suggested diagrams:

- System context (mobile ↔ API ↔ external services)
- Push notification registration sequence
- RevenueCat webhook flow
- Environment promotion (dev → staging → production)